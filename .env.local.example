# Eggo Environment Variables
# Copy this file to .env.local and fill in your values
#
# For Netlify deployment: Add these same variables in your Netlify dashboard
# under Site Settings > Environment Variables

# ============================================================================
# Firebase Configuration
# Get these values from: Firebase Console > Project Settings > General
# ============================================================================
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# ============================================================================
# Brickset API (recommended - better data coverage)
# Get your API key from: https://brickset.com/tools/webservices/requestkey
# Note: This is a server-only variable (not exposed to browser)
# ============================================================================
BRICKSET_API_KEY=your-brickset-api-key

# ============================================================================
# Rebrickable API (alternative to Brickset)
# Only used if NEXT_PUBLIC_USE_REBRICKABLE=true
# Get your API key from: https://rebrickable.com/api/
# ============================================================================
# NEXT_PUBLIC_USE_REBRICKABLE=true
NEXT_PUBLIC_REBRICKABLE_API_KEY=your-rebrickable-api-key

# ============================================================================
# Firebase Admin SDK (required for high-resolution background removal)
# Processed images are uploaded to Firebase Storage instead of stored as base64
# Get these values from: Firebase Console > Project Settings > Service Accounts
# Click "Generate new private key" and copy values from the JSON file
# ============================================================================
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----\n"

# ============================================================================
# rembg.com API (optional - for background removal on set images)
# Get your API key from: https://www.rembg.com
# Note: Free tier has 460x460 resolution limit. Upgrade for full resolution.
# ============================================================================
# REMBG_API_KEY=your-rembg-api-key
